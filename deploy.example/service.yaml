apiVersion: v1
kind: Service
metadata:
  name: <AGENT_NAME>
  namespace: <NAMESPACE>
spec:
  selector:
    app: <AGENT_NAME>
  ports:
    - port: <PORT>
      targetPort: <PORT>
      name: a2a
  type: ClusterIP
